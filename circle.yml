general:
  build_dir: src

checkout:
  post:
    - git submodule sync
    - git submodule update --init --recursive


checkout:
  post:
    - git submodule sync
    - git submodule update --init --recursive

test:
  override:
    - ./test.sh
